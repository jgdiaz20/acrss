# Use Procfile for non-Docker deployments only
# For Docker deployments to Render, use render.yaml instead
# This file is kept for backwards compatibility

web: php artisan serve --host=0.0.0.0 --port=$PORT